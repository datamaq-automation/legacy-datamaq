# Variables de entorno para profebustos-www
# Copiá este archivo a `.env` y completá los valores reales según cada entorno.
# La configuración pública del sitio vive en `src/infrastructure/content/content.ts`.

# Header de verificacion de origen requerido por el backend
VITE_ORIGIN_VERIFY_SECRET=