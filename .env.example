# Variables de entorno para datamaq-www
# Copi� este archivo a `.env` y complet� los valores reales seg�n cada entorno.
# La configuraci�n p�blica del sitio vive en `src/infrastructure/content/content.ts`.

# Header de verificacion de origen requerido por el backend
VITE_ORIGIN_VERIFY_SECRET=